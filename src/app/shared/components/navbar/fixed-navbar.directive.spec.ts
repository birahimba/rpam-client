import { FixedNavbarDirective } from './fixed-navbar.directive';

describe('FixedNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new FixedNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
